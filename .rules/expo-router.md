# Expo Router routes

Use Expo Router with a strict folder-per-route structure. Every route must be a directory with an `index.tsx`, for example `settings/index.tsx` and `settings/notifications/index.tsx`. Do not create standalone route files such as `settings.tsx` or `notifications.tsx`.

Keep `_layout.tsx` only for actual navigation or layout logic.
